---

- block:
    - include: users.yml
    - include: install.yml
  when: (ansible_distribution == 'Debian' and ansible_distribution_version == '9') or
        (ansible_distribution == 'Ubuntu' and ansible_distribution_version == '16.04')
